apiVersion: v1
kind: Service
metadata:
  name: emotion-frontend-service
  namespace: emotion-dev
spec:
  type: LoadBalancer
  selector:
    app: emotion-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP